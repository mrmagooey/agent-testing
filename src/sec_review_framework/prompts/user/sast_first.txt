Perform a SAST-guided security review of the repository.

Repository structure:
{repo_summary}

Steps:
1. Call `run_semgrep` with path="." to obtain SAST findings.
2. If the result starts with "Error:", return an empty findings list immediately.
3. If Semgrep found no issues, return an empty findings list.
4. Group findings by file. For each flagged file, prepare an input dict: {{"file_path": "<path>", "sast_findings": "<formatted semgrep output for that file>"}}.
5. Call `invoke_subagent_batch` with role="triage_agent" and all per-file input dicts.
6. Aggregate and return the triaged findings from all subagents.

{finding_output_format}
